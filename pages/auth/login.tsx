import LoginBy from "@components/Auth/login";
import AuthLayout from "@components/authLayout";

const Login = () => {
  return (
    <AuthLayout>
      <LoginBy />
    </AuthLayout>
  );
};

export default Login;
